import 'package:flutter/material.dart';

class SettingSrn extends StatelessWidget {
  const SettingSrn({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFF04FBC3),
      child: SafeArea(child: Scaffold()),
    );
  }
}

class NotificationSrn extends StatelessWidget {
  const NotificationSrn({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFF04FBC3),
      child: SafeArea(child: Scaffold()),
    );
  }
}

class FavoriterSrn extends StatelessWidget {
  const FavoriterSrn({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFF04FBC3),
      child: SafeArea(child: Scaffold()),
    );
  }
}
